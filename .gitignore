# Rust
/target/
/artifacts/
/dist/
**/*.rs.bk

# Cargo.lock 不忽略：本仓库产出二进制，锁文件须随仓库以保证可复现构建。

# 一次性 APFS 探针的编译产物（probe_attrs.c 本身要提交，二进制不提交）
/probe_attrs
*.o
*.dSYM/

# macOS
.DS_Store
# 例外：fixture 里的 .DS_Store 是被测条件本身。散落在 DerivedData / Archives /
# iOS DeviceSupport 里的非目录条目曾使整个通配展开中断，丢掉该类别的全部 store。
!tests/fixtures/**/.DS_Store
